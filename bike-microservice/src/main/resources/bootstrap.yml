server:
  port: 8083

spring:
  application:
    name: bike-service
  cloud:
    config:
      enabled: true
      uri: http://localhost:8080
  jpa:
    properties:
      hibernate:
        globally_quoted_identifiers: true
