spring:
  application:
    name: car-microservice
  cloud:
    config:
      enabled: true
      uri: http://localhost:8080
  jpa:
    properties:
      hibernate:
        globally_quoted_identifiers: true