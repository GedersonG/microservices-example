spring:
  application:
    name: user-microservice
  cloud:
    config:
      enabled: true
      uri: http://localhost:8080
  jpa:
    properties:
      hibernate:
        globally_quoted_identifiers: true